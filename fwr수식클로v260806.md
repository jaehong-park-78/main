````
정규화: F%, W%, C%는 0~100 표시값. 수식에서는 100으로 나눠 0~1 스케일로 맞춤.

f = F% / 100        (0~1)
w = W% / 100        (0~1)
c = C% / 100         (-1~1)

E = min(f, w) × c × η × (1 + T/(1+αT))

crisis ⟺ E ≤ γ·T

평소 (crisis = 거짓):
    T(내일) = T(오늘) + |E| − γ·T(오늘)
    W(내일) = W(오늘)

위기 (crisis = 참):
    T(내일) = β × T(오늘)
    W(내일) = W*        ← 새 장비를 뭘 고를지는 이 계산법이 정해주지 않음

----
# FWR 일일 트래커 — 계산식

---

## 1. 정규화

F%, W%, C%는 0~100 사이의 자기평가 표시값이다. 계산에 쓸 때는 아래처럼 스케일을 맞춘다.

```
f = F% / 100      (0 ~ 1)
w = W% / 100      (0 ~ 1)
c = C% / 100      (-1 ~ 1)
```

---

## 2. 존재 방정식

```
E = min(f, w) × c × η × (1 + T/(1+αT))
```

- **f, w**: 오늘의 흐름(F)과 구조(W). 둘 중 더 약한 쪽이 E를 제한한다 (min).
- **c**: 정렬도. 음수면 위상 상쇄(내부 요소들이 서로 반대 방향).
- **η**: 효율 (0~1).
- **T**: 축적된 역사. α는 역사 증폭의 포화 계수.

---

## 3. 위기 판정

```
crisis ⟺ E ≤ γ·T
```

오늘의 존재 상태(E)가 축적된 역사(T)를 망각률(γ)만큼 깎아낸 값보다 낮거나 같으면 위기로 판정한다.

---

## 4. 일일 업데이트 규칙

### 평소 (crisis = 거짓)

```
T(내일) = T(오늘) + |E| − γ·T(오늘)
W(내일) = W(오늘)
```

역사는 오늘의 존재값만큼 쌓이고, 망각률만큼 깎인다. 구조는 그대로 유지.

### 위기 (crisis = 참)

```
T(내일) = β × T(오늘)
W(내일) = W*
```

역사는 β 비율만큼만 계승되고 나머지는 리셋된다. 구조는 W*로 전환된다.

> **주의**: W\*(위기 시 전환할 새 구조)는 이 계산법이 정해주지 않는다.
> 무엇으로 바꿀지는 별도로 판단해야 한다.

---

## 5. 경계값

```
T_max ≈ (1 + 1/α) / γ
```

T는 이 값을 넘지 않고 수렴한다. (R ≤ 1+1/α이고 dT/dt = |R|−γT이므로, T가 T_max를 넘으면
자동으로 감소하는 구조 — 정리2와 동일한 형태)

---

## 6. 안정화 조건

위기가 다시 발생하지 않으면 → **안정화 성공**.

---

## 파라미터 요약

| 기호 | 의미 | 범위 |
|---|---|---|
| f, w | 흐름 / 구조 (오늘의 자기평가) | 0~1 |
| c | 정렬도 | -1~1 |
| η | 효율 | 0~1 |
| T | 축적된 역사 | ≥0, T_max로 수렴 |
| α | 역사 증폭 포화 계수 | 자기 조정 |
| γ | 망각률 | 자기 조정 |
| β | 위기 시 역사 계승 비율 | 0~1, 자기 조정 |
| W\* | 위기 시 전환할 새 구조 | 이 계산법이 정하지 않음 — 별도 판단 |

경계값:
    T의 최댓값 ≈ (1 + 1/α) / γ   (T는 이 값을 넘지 않고 수렴함)

안정화: 위기가 다시는 안 오면 → 안정화 성공
````
